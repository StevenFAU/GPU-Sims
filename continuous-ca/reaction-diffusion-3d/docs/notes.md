# Reaction-Diffusion 3D — Iteration notes

A scratch file for design-iteration observations as the sim is tuned post-build.
Empty at Phase 3 ship; populated as the user lives with the running sim and
notices things to revisit.

Suggested headings to add later:
- Visual notes per Pearson preset (which look most compelling at 256³, which want different default raymarch step counts, etc.)
- Performance hot spots discovered during profiling
- Boundary effects observed during free-fly inspection (the periodic torus has visible wraparound at certain camera angles)
- Ideas worth promoting to the v1.1 spec
